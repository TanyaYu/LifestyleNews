<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:parentTag="android.widget.LinearLayout">

    <Space
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <ImageView
        android:id="@+id/icon"
        android:layout_width="@dimen/icon_size_xh"
        android:layout_height="@dimen/icon_size_xh"
        android:layout_marginBottom="@dimen/spacing_h"
        android:contentDescription="@string/content_description_empty_view"
        tools:src="@drawable/ic_search" />

    <TextView
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:gravity="center_horizontal"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/spacing_xxxh"
        android:layout_marginRight="@dimen/spacing_xxxh"
        android:textAppearance="@style/Subtitle"
        tools:text="Please try again later Please try again later Please try again later Please try again later" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/action"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_m"
        android:text="@string/retry_action" />

    <Space
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />
</merge>